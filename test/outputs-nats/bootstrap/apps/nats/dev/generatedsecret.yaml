apiVersion: deployments.plural.sh/v1alpha1
kind: GeneratedSecret
metadata:
  name: nats-users-dev
  namespace: infra
spec:
  template:
    username: "nats"
    password: "nats_{{ 48 | randAlphaNum }}"
  destinations:
  - name: nats-users-dev
    namespace: infra
  - name: nats-users-dev
    namespace: plrl-deploy-operator

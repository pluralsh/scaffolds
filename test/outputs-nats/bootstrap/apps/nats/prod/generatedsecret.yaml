apiVersion: deployments.plural.sh/v1alpha1
kind: GeneratedSecret
metadata:
  name: nats-users-prod
  namespace: infra
spec:
  template:
    username: "nats"
    password: "nats_{{ 48 | randAlphaNum }}"
  destinations:
  - name: nats-users-prod
    namespace: infra
  - name: nats-users-prod
    namespace: plrl-deploy-operator

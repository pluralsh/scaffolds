apiVersion: deployments.plural.sh/v1alpha1
kind: GeneratedSecret
metadata:
  name: valkey-users-dev
  namespace: infra
spec:
  template:
    password: "{{ 48 | randAlphaNum }}"
  destinations:
  - name: valkey-users-dev
    namespace: infra
  - name: valkey-users-dev
    namespace: plrl-deploy-operator

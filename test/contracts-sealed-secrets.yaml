apiVersion: platform.plural.sh/v1alpha1
kind: PrContracts
metadata:
  name: sealed-secrets
spec:
  workdir: test/outputs-sealed-secrets
  templates:
    from: catalogs/security/sealed-secrets
    to: test/outputs-sealed-secrets/catalogs/security/sealed-secrets
  automations:
  - file: ../../setup/catalogs/security/sealed-secrets.yaml
    context: ../contexts/sealed-secrets.yaml

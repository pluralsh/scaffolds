{% raw %}
{% assign imports_bedrock_key =  'bedrock-' | append: configuration.cluster %}

config:
  provider: bedrock

serviceAccount:
  create: true
  name: ai-proxy
  namespace: bedrock
  annotations:
    eks.amazonaws.com/role-arn:  {{ imports[imports_bedrock_key].bedrock_role_arn }}
{% endraw %}

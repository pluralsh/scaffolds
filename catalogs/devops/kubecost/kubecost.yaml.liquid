global:
  grafana:
    enabled: false
    proxy: false

kubecostFrontend:
  enabled: true

serviceMonitor:
  enabled: true

server:
  global:
    external_labels:
      cluster_id: {{ context.cluster }}

global:
  clusterId: {% raw %}{{ cluster.handle }}{% endraw %}
  grafana:
    enabled: false
    proxy: false

kubecostFrontend:
  enabled: true

serviceMonitor:
  enabled: true

server:
  global:
    external_labels:
      cluster_id: {% raw %}{{ cluster.handle }}{% endraw %}

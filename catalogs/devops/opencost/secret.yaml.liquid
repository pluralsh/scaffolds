{% if context.auth == 'credentials' %}
apiVersion: v1
kind: Secret
metadata:
  name: opencost-prometheus-auth
  namespace: opencost
type: Opaque
stringData:
  username: '{{ context.username }}'
  password: '{{ context.password }}'
{% endif %}

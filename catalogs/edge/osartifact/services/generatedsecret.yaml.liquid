{% raw %}
apiVersion: deployments.plural.sh/v1alpha1
kind: GeneratedSecret
metadata:
  name: plural-edge-{{ configuration.name }}
spec:
  destinations:
    - name: plural-edge-{{ configuration.name }}-ssh
  template:
    {% assign inner_raw = "{% raw %}password: {{ 32 |  randAlphaNum }}{% endraw %}" %}
    {{ inner_raw }}
{% endraw %}